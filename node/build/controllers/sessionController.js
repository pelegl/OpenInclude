// Generated by CoffeeScript 1.6.1
var SessionController, esClient, get_models, _ref,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

_ref = require('../conf'), esClient = _ref.esClient, get_models = _ref.get_models;

SessionController = (function(_super) {

  __extends(SessionController, _super);

  function SessionController(req, res) {
    this.req = req;
    this.res = res;
    if (this.req.xhr) {
      SessionController.__super__.constructor.apply(this, arguments);
    } else {
      this.res.redirect("/");
    }
  }

  SessionController.prototype.index = function() {
    if (this.req.user) {
      return this.res.json(this.req.user.public_info());
    } else {
      return this.res.json({
        is_authenticated: false
      });
    }
  };

  return SessionController;

})(require('./basicController'));

module.exports = function(req, res) {
  return new SessionController(req, res);
};
